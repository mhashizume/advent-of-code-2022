input_file = File.readlines('input')


input_file.scan(/[[:upper:]]/)

# can't iterate over a string
# only returns the first match
y.each {|i| i.index(i.match(/[[:upper:]]/).to_s) }

match caps
count whitespace?
match 

# count number of arrays
input_file.each { |line|
  if line.match?(/^\s\d/)
    array_numbers = line.strip.split(/\s+/)

  end
  return
}


# create arrays of each of the columns
#   gather capital letters in arrays
#   ensure they're are properly labeled

# parse the instructions to shift/push to arrays